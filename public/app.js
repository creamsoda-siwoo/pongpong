// 퐁퐁복음 10장 데이터 정의
const gospelChapters = [
  {
    title: "퐁퐁복음 제 1장",
    verses: [
      "지구의 모든 순간과 자연 또는 문화는 모두 퐁퐁푸린에 의해 창조되었다. 지금 이 순간도 모두 퐁퐁푸린에 의해 일어나는 일이라 하셨다.",
      "퐁퐁푸린의 적이자 가장 악한 퐁퐁푸틴은 심성이 악하며 도덕성과 사회성 결핍 증상이 있다. 퐁퐁푸린이 이르시되 그는 자신의 힘으로는 힘겹게 이길수 있으며 핵폭탄을 가지고 있다 하셨다.",
      "이르되, 이 세상에는 선하면 호구가 되리라. 그러나 이 퐁퐁푸린은 악한 존재보다 선한 존재가 강하다는 것을 증명하셨다.",
      "퐁퐁푸린과 퐁퐁푸틴의 전투중 총퐁푸틴을 처치했지만 퐁퐁푸린에겐 약점이 생겼다. 이르되, 그 약점은 아무에게도 알려지지 않았다. 그러나, 추측에 따르면 지적능력에 큰 영향이 갔더라. 왜냐하면 퐁퐁푸린님이 이르시되, 내 사회점수가 84점이다 라고 하시니.",
      "이 이야기는 두 자의 전투에 관한 설화이다. 두 자가 서로를 바라보니 공기가 떨리고 들판의 푸딩도 흔들렸더라.",
      "이로써 첫 대립이 시작되었더라."
    ]
  },
  {
    title: "퐁퐁복음 제 2장",
    verses: [
      "퐁퐁푸틴이 그림자 군세를 모아 성을 에워쌌더라.",
      "퐁퐁푸린은 친구들과 함께 두려움을 나누어 반으로 줄였더라.",
      "퐁퐁푸틴의 함성은 우레 같았으나 마음은 흔들렸더라.",
      "퐁퐁푸린이 미소를 지으니 동료들의 사기가 높아졌더라.",
      "첫 충돌이 벌어졌으나 승패는 나지 아니하였더라.",
      "양측은 다시 힘을 기르기로 하였더라."
    ]
  },
  {
    title: "퐁퐁복음 제 3장",
    verses: [
      "퐁퐁푸틴은 거대한 검은 푸딩 병기를 만들었더라.",
      "퐁퐁푸린은 지혜로 이를 상대하려 하였더라.",
      "힘만으로는 모든 것을 얻지 못함을 사람들이 보기 시작하였더라.",
      "퐁퐁푸틴은 더욱 조급해졌더라.",
      "퐁퐁푸린은 빈틈을 찾되 서두르지 아니하였더라.",
      "전장의 흐름이 서서히 바뀌기 시작하였더라."
    ]
  },
  {
    title: "퐁퐁복음 제 4장",
    verses: [
      "퐁퐁푸틴이 거센 돌격을 감행하였더라.",
      "퐁퐁푸린은 이를 정면으로 받지 아니하였더라.",
      "작은 승리들이 모여 큰 희망이 되었더라.",
      "퐁퐁푸틴의 갑옷에 처음으로 금이 갔더라.",
      "그러나 그 약점은 누구도 눈치채지 못하였더라.",
      "전쟁은 더욱 길어졌더라."
    ]
  },
  {
    title: "퐁퐁복음 제 5장",
    verses: [
      "퐁퐁푸린은 동료들과 함께 작전을 세웠더라.",
      "퐁퐁푸틴은 자신의 힘만을 믿었더라.",
      "협력이 고독보다 강함이 드러났더라.",
      "퐁퐁푸틴의 군세가 흔들리기 시작하였더라.",
      "그러나 마지막 힘은 아직 남아 있었더라.",
      "모두가 결전을 준비하였더라."
    ]
  },
  {
    title: "퐁퐁복음 제 6장",
    verses: [
      "하늘에 먹구름이 모이고 바람이 멎었더라.",
      "퐁퐁푸틴이 최후의 힘을 꺼내 들었더라.",
      "퐁퐁푸린도 물러서지 아니하였더라.",
      "두 힘이 부딪히니 산천이 울렸더라.",
      "빛과 어둠이 뒤섞여 모두가 눈을 감았더라.",
      "승패는 아직 결정되지 아니하였더라."
    ]
  },
  {
    title: "퐁퐁복음 제 7장",
    verses: [
      "연기가 걷히매 두 자 모두 서 있었더라.",
      "퐁퐁푸린은 지쳤으나 의지를 잃지 아니하였더라.",
      "퐁퐁푸틴 또한 큰 상처를 입었더라.",
      "서로의 마지막 일격이 준비되었더라.",
      "모두가 숨을 죽이고 바라보았더라.",
      "결전의 순간이 이르렀더라."
    ]
  },
  {
    title: "퐁퐁복음 제 8장",
    verses: [
      "퐁퐁푸린의 마지막 일격이 빛처럼 뻗었더라.",
      "퐁퐁푸틴도 모든 힘을 모아 맞섰더라.",
      "거대한 충격이 세상을 뒤흔들었더라.",
      "퐁퐁푸틴의 무기가 산산이 부서졌더라.",
      "퐁퐁푸틴이 무릎을 꿇었더라.",
      "전쟁은 끝을 향해 나아갔더라."
    ]
  },
  {
    title: "퐁퐁복음 제 9장",
    verses: [
      "퐁퐁푸틴이 자신의 패배를 인정하였더라.",
      "퐁퐁푸린은 승리를 자랑하지 아니하였더라.",
      "강함보다 품격이 오래 남음을 모두가 알게 되었더라.",
      "전장은 다시 평온을 되찾았더라.",
      "사람들은 서로를 도우며 웃음을 되찾았더라.",
      "긴 전쟁이 마침내 끝났더라."
    ]
  },
  {
    title: "퐁퐁복음 제 10장",
    verses: [
      "퐁퐁푸린은 평화를 지키는 것이 승리보다 어렵다 하였더라.",
      "사람들은 그 말을 오래 기억하였더라.",
      "퐁퐁푸틴도 지난 일을 돌아보며 침묵하였더라.",
      "세상에는 다시 웃음과 푸딩 향기가 가득하였더라.",
      "훗날 이 이야기를 들은 자마다 용기와 우정을 배우게 되었더라.",
      "이로써 퐁퐁복음 열 장의 기록이 마쳐졌더라. 아멘."
    ]
  }
];

// 글로벌 애플리케이션 상태
const state = {
  user: null,
  isLoggedIn: false,
  globalWorshipCount: 0,
  userWorshipCount: 0,
  activeChapter: 0
};

let selectedImageData = null;

// DOM 요소 캐시
const el = {
  bgParticles: document.getElementById('bgParticles'),
  authSection: document.getElementById('authSection'),
  worshipButton: document.getElementById('worshipButton'),
  globalWorshipCount: document.getElementById('globalWorshipCount'),
  userWorshipCount: document.getElementById('userWorshipCount'),
  userStatCard: document.getElementById('userStatCard'),
  loginPrompt: document.getElementById('loginPrompt'),
  chapterNav: document.getElementById('chapterNav'),
  currentChapterTitle: document.getElementById('currentChapterTitle'),
  gospelContent: document.getElementById('gospelContent'),
  boardWriteContainer: document.getElementById('boardWriteContainer'),
  boardFeed: document.getElementById('boardFeed'),
  rankList: document.getElementById('rankList'),
  loginForm: document.getElementById('loginForm'),
  registerForm: document.getElementById('registerForm')
};

// --- 초기화 작업 ---
document.addEventListener('DOMContentLoaded', () => {
  createBackgroundPuddings();
  initGospel();
  checkAuth();
  fetchStats();
  fetchLeaderboard();
  fetchPosts();

  // 5초 간격 실시간 갱신 폴링
  setInterval(() => {
    fetchStats();
    fetchLeaderboard();
    fetchPosts();
  }, 5000);

  // 숭배 버튼 클릭 이벤트 등록
  if (el.worshipButton) {
    el.worshipButton.addEventListener('click', handleWorshipClick);
  }

  // 폼 제출 이벤트 등록
  if (el.loginForm) el.loginForm.addEventListener('submit', handleLogin);
  if (el.registerForm) el.registerForm.addEventListener('submit', handleRegister);
});

// 배경에 둥둥 떠다니는 푸딩 입자 생성
function createBackgroundPuddings() {
  const count = 12;
  for (let i = 0; i < count; i++) {
    const pudding = document.createElement('div');
    pudding.classList.add('bg-pudding');
    
    // 무작위 위치 및 애니메이션 속성 지정
    pudding.style.left = `${Math.random() * 100}vw`;
    pudding.style.width = pudding.style.height = `${Math.random() * 40 + 30}px`;
    pudding.style.animationDelay = `${Math.random() * -20}s`;
    pudding.style.animationDuration = `${Math.random() * 15 + 15}s`;
    
    el.bgParticles.appendChild(pudding);
  }
}

// 퐁퐁복음 렌더링
function initGospel() {
  el.chapterNav.innerHTML = '';
  gospelChapters.forEach((chapter, index) => {
    const btn = document.createElement('button');
    btn.className = `chapter-btn ${index === 0 ? 'active' : ''}`;
    btn.innerHTML = `
      <span>제 ${index + 1}장</span>
      <i class="fa-solid fa-angle-right"></i>
    `;
    btn.addEventListener('click', () => {
      // 액티브 클래스 교체
      document.querySelectorAll('.chapter-btn').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      renderChapter(index);
    });
    el.chapterNav.appendChild(btn);
  });
  renderChapter(0);
}

function renderChapter(index) {
  state.activeChapter = index;
  const chapter = gospelChapters[index];
  el.currentChapterTitle.innerText = chapter.title;
  
  el.gospelContent.innerHTML = chapter.verses
    .map((verse, vIdx) => `
      <div class="gospel-verse">
        <span class="gospel-verse-num">${vIdx + 1}절</span>
        ${verse}
      </div>
    `).join('');
}

// --- 로그인 상태 체크 및 UI 업데이트 ---
async function checkAuth() {
  try {
    const res = await fetch('/api/auth/me');
    const data = await res.json();
    
    if (data.loggedIn) {
      state.isLoggedIn = true;
      state.user = data.user;
      state.userWorshipCount = data.user.worship_count;
      updateAuthUI();
    } else {
      state.isLoggedIn = false;
      state.user = null;
      updateAuthUI();
    }
  } catch (err) {
    console.error('인증 확인 실패:', err);
  }
}

function updateAuthUI() {
  if (state.isLoggedIn && state.user) {
    // 헤더 로그인 영역 변경
    el.authSection.innerHTML = `
      <div class="user-badge">
        🍮 <strong>${state.user.username}</strong> 신도
      </div>
      <button class="btn btn-secondary btn-logout" onclick="handleLogout()">로그아웃</button>
    `;

    // 숭배 카운터 보드 업데이트
    el.userWorshipCount.innerText = state.userWorshipCount.toLocaleString();
    el.loginPrompt.style.display = 'none';

    // 게시판 작성 폼 렌더링
    el.boardWriteContainer.innerHTML = `
      <form id="postForm" onsubmit="handleWritePost(event)">
        <div class="form-group">
          <label for="postContent"><i class="fa-solid fa-comment-dots"></i> 한마디 올리기</label>
          <textarea id="postContent" class="form-control" placeholder="경배의 메시지나 소원을 빌어보세요!" required maxlength="150"></textarea>
        </div>
        <div class="form-group" style="margin-top: 10px;">
          <label for="postImage"><i class="fa-solid fa-image"></i> 이미지 첨부 (선택)</label>
          <input type="file" id="postImage" class="form-control" accept="image/*" onchange="previewImage(event)" style="padding: 5px 12px; font-size: 13px;">
          <div id="imagePreviewContainer" class="image-preview-container" style="display: none;"></div>
        </div>
        <button type="submit" class="btn btn-primary btn-block" style="margin-top: 10px;">방명록 기록</button>
      </form>
    `;
  } else {
    // 비로그인 헤더
    el.authSection.innerHTML = `
      <button class="btn btn-secondary" onclick="openModal('loginModal')">로그인</button>
      <button class="btn btn-primary" onclick="openModal('registerModal')">회원가입</button>
    `;

    // 숭배 카운터 보드 초기화
    el.userWorshipCount.innerText = '0';
    el.loginPrompt.style.display = 'block';

    // 게시판 작성 폼을 잠금 메시지로 표시
    el.boardWriteContainer.innerHTML = `
      <p class="login-notice"><i class="fa-solid fa-lock"></i> 숭배 글을 작성하려면 <a href="javascript:void(0)" onclick="openModal('loginModal')">로그인</a>이 필요합니다.</p>
    `;
  }
}

// --- 통계 & 게시글 데이터 조회 API 통신 ---

async function fetchStats() {
  try {
    const res = await fetch('/api/worship/stats');
    const data = await res.json();
    
    if (data) {
      state.globalWorshipCount = data.globalCount;
      el.globalWorshipCount.innerText = parseInt(data.globalCount).toLocaleString();
      
      if (state.isLoggedIn) {
        state.userWorshipCount = data.userCount;
        el.userWorshipCount.innerText = parseInt(data.userCount).toLocaleString();
      }
    }
  } catch (err) {
    console.error('통계 갱신 오류:', err);
  }
}

async function fetchLeaderboard() {
  try {
    const res = await fetch('/api/worship/leaderboard');
    const list = await res.json();
    
    el.rankList.innerHTML = '';
    if (list.length === 0) {
      el.rankList.innerHTML = `<p style="text-align: center; color: var(--dark-brown-light);">아직 숭배를 바친 신도가 없습니다.</p>`;
      return;
    }

    list.forEach((item, index) => {
      const row = document.createElement('div');
      row.className = 'rank-item';
      row.innerHTML = `
        <div class="rank-left">
          <div class="rank-num">${index + 1}</div>
          <div class="rank-name">${item.username}</div>
        </div>
        <div class="rank-count">${parseInt(item.worship_count).toLocaleString()} <span>번</span></div>
      `;
      el.rankList.appendChild(row);
    });
  } catch (err) {
    console.error('리더보드 갱신 오류:', err);
  }
}

async function fetchPosts() {
  try {
    const res = await fetch('/api/posts');
    const list = await res.json();
    
    el.boardFeed.innerHTML = '';
    if (list.length === 0) {
      el.boardFeed.innerHTML = `<p style="text-align: center; padding: 40px 0; color: var(--dark-brown-light);">아직 작성된 방명록이 없습니다.</p>`;
      return;
    }

    list.forEach(post => {
      const item = document.createElement('div');
      item.className = 'post-item';
      const formattedDate = new Date(post.created_at).toLocaleString('ko-KR', {
        month: 'short',
        day: 'numeric',
        hour: '2-digit',
        minute: '2-digit'
      });
      
      let imageHtml = '';
      if (post.image_data) {
        imageHtml = `
          <div class="post-image-container">
            <img src="${post.image_data}" class="post-image" alt="첨부 이미지" onclick="window.open('${post.image_data}')">
          </div>
        `;
      }
      
      item.innerHTML = `
        <div class="post-header">
          <div class="post-author">${post.username}</div>
          <div class="post-date">${formattedDate}</div>
        </div>
        <div class="post-content">${escapeHTML(post.content)}</div>
        ${imageHtml}
      `;
      el.boardFeed.appendChild(item);
    });
  } catch (err) {
    console.error('게시글 불러오기 오류:', err);
  }
}

// HTML 이스케이프 유틸리티
function escapeHTML(str) {
  return str.replace(/[&<>'"]/g, 
    tag => ({
      '&': '&amp;',
      '<': '&lt;',
      '>': '&gt;',
      "'": '&#39;',
      '"': '&quot;'
    }[tag] || tag)
  );
}

// --- 숭배 버튼 클릭 액션 ---
async function handleWorshipClick(e) {
  // 신단 흔들림 애니메이션 효과 적용
  el.worshipButton.classList.remove('shake-animation');
  void el.worshipButton.offsetWidth; // 리플로우 트리거
  el.worshipButton.classList.add('shake-animation');

  // 클릭 위치 근처에 숭배 Particle 띄우기
  createFloatingParticle(e);

  try {
    const res = await fetch('/api/worship', { method: 'POST' });
    const data = await res.json();
    
    if (data.success) {
      state.globalWorshipCount = data.globalCount;
      el.globalWorshipCount.innerText = parseInt(data.globalCount).toLocaleString();
      
      if (state.isLoggedIn && data.userCount !== undefined && data.userCount !== null) {
        state.userWorshipCount = data.userCount;
        el.userWorshipCount.innerText = parseInt(data.userCount).toLocaleString();
      }
    }
  } catch (err) {
    console.error('숭배 요청 전송 오류:', err);
  }
}

// 클릭된 좌표에 둥둥 떠다니는 +1 입자 효과 생성
function createFloatingParticle(e) {
  const rect = el.worshipButton.getBoundingClientRect();
  const particle = document.createElement('div');
  particle.className = 'worship-particle';
  
  // 무작위 이펙트 텍스트 선택
  const praiseTexts = ['+1', '🍮', '숭배', '아멘', 'Purin!', 'Pudding!'];
  particle.innerText = praiseTexts[Math.floor(Math.random() * praiseTexts.length)];
  
  // 마우스 위치 기준으로 파티클 띄우기, 마우스 없으면 정중앙
  let x = rect.left + rect.width / 2;
  let y = rect.top + rect.height / 3;
  if (e && e.clientX && e.clientY) {
    x = e.clientX;
    y = e.clientY;
  }
  
  particle.style.left = `${x}px`;
  particle.style.top = `${y}px`;
  
  // 날아갈 좌우 방향 무작위 지정
  const xDir = `${(Math.random() - 0.5) * 80}px`;
  particle.style.setProperty('--x-dir', xDir);
  
  document.body.appendChild(particle);
  
  // 1초 뒤 요소 제거
  setTimeout(() => {
    particle.remove();
  }, 1000);
}

// --- 인증 폼 제출 핸들러 ---

async function handleRegister(e) {
  e.preventDefault();
  const username = document.getElementById('registerUsername').value;
  const password = document.getElementById('registerPassword').value;

  try {
    const res = await fetch('/api/auth/register', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username, password })
    });
    
    const data = await res.json();
    if (res.ok && data.success) {
      alert('성공적으로 가입되었습니다. 방금 생성한 계정으로 로그인해 주세요.');
      closeModal('registerModal');
      openModal('loginModal');
      el.registerForm.reset();
    } else {
      alert(data.error || '회원가입에 실패했습니다.');
    }
  } catch (err) {
    console.error(err);
    alert('서버와 통신 중 오류가 발생했습니다.');
  }
}

async function handleLogin(e) {
  e.preventDefault();
  const username = document.getElementById('loginUsername').value;
  const password = document.getElementById('loginPassword').value;

  try {
    const res = await fetch('/api/auth/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ username, password })
    });
    
    const data = await res.json();
    if (res.ok && data.success) {
      state.isLoggedIn = true;
      state.user = data.user;
      state.userWorshipCount = data.user.worship_count;
      updateAuthUI();
      closeModal('loginModal');
      el.loginForm.reset();
      
      // 로그인했으므로 리더보드 및 전반적인 계수 갱신
      fetchStats();
      fetchLeaderboard();
    } else {
      alert(data.error || '로그인에 실패했습니다.');
    }
  } catch (err) {
    console.error(err);
    alert('서버와 통신 중 오류가 발생했습니다.');
  }
}

async function handleLogout() {
  try {
    const res = await fetch('/api/auth/logout', { method: 'POST' });
    const data = await res.json();
    
    if (data.success) {
      state.isLoggedIn = false;
      state.user = null;
      updateAuthUI();
      fetchStats();
    }
  } catch (err) {
    console.error('로그아웃 오류:', err);
  }
}

// 방명록 작성 제출 핸들러
async function handleWritePost(e) {
  e.preventDefault();
  const contentEl = document.getElementById('postContent');
  const content = contentEl.value;
  const image_data = selectedImageData;

  try {
    const res = await fetch('/api/posts', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ content, image_data })
    });
    
    const data = await res.json();
    if (res.ok && data.success) {
      contentEl.value = '';
      removePreview();
      fetchPosts(); // 피드 목록 갱신
    } else {
      alert(data.error || '글 작성에 실패했습니다.');
    }
  } catch (err) {
    console.error(err);
    alert('방명록 게시 도중 오류 발생');
  }
}

// --- 모달 다이얼로그 제어 ---

window.openModal = function(modalId) {
  const modal = document.getElementById(modalId);
  if (modal) {
    modal.style.display = 'flex';
    void modal.offsetWidth; // 강제 리플로우
    modal.classList.add('show');
  }
};

window.closeModal = function(modalId) {
  const modal = document.getElementById(modalId);
  if (modal) {
    modal.classList.remove('show');
    setTimeout(() => {
      modal.style.display = 'none';
    }, 300);
  }
};

window.switchModal = function(closeId, openId) {
  closeModal(closeId);
  setTimeout(() => {
    openModal(openId);
  }, 300);
};

// 모달 외부 클릭 시 닫기
window.addEventListener('click', (e) => {
  if (e.target.classList.contains('modal')) {
    closeModal(e.target.id);
  }
});

// --- 이미지 미리보기 및 Base64 변환 ---
window.previewImage = function(event) {
  const file = event.target.files[0];
  if (!file) return;

  // 크기 제한: 1.5MB
  if (file.size > 1.5 * 1024 * 1024) {
    alert("이미지 크기는 1.5MB 이하만 업로드 가능합니다.");
    event.target.value = "";
    return;
  }

  const reader = new FileReader();
  reader.onload = function(e) {
    selectedImageData = e.target.result;
    const container = document.getElementById("imagePreviewContainer");
    container.innerHTML = `
      <img src="${selectedImageData}" class="image-preview" alt="Preview">
      <button type="button" class="image-preview-remove" onclick="removePreview()">&times;</button>
    `;
    container.style.display = "inline-block";
  };
  reader.readAsDataURL(file);
};

window.removePreview = function() {
  selectedImageData = null;
  const input = document.getElementById("postImage");
  if (input) input.value = "";
  const container = document.getElementById("imagePreviewContainer");
  if (container) {
    container.innerHTML = "";
    container.style.display = "none";
  }
};

// --- 오늘의 푸린 신탁 로직 ---
window.revealOracle = async function() {
  const card = document.getElementById("oracleCard");
  if (!card || card.classList.contains("flipped")) return;

  try {
    const res = await fetch('/api/oracle');
    const data = await res.json();

    document.getElementById("oracleScore").innerText = data.purityScore;
    document.getElementById("oracleText").innerText = `"${data.oracle}"`;
    document.getElementById("oraclePudding").innerText = data.luckyPudding;

    card.classList.add("flipped");
  } catch (err) {
    console.error("신탁 불러오기 실패:", err);
    alert("신탁을 불러오는 도중 오류가 발생했습니다.");
  }
};

window.resetOracleCard = function() {
  const card = document.getElementById("oracleCard");
  if (card) {
    card.classList.remove("flipped");
  }
};
